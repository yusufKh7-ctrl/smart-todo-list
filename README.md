# smart-todo-list
A clean CLI To-Do List in Python (OOP + JSON)

Tasks are automatically saved to and loaded from `Tasks.json`, so your list persists between sessions.

### Features
- Add multiple tasks at once
- Mark tasks as complete ✓
- Delete tasks or clear everything
- Auto-save & auto-load

### How to run
```bash
python main.py